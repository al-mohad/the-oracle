import 'package:flutter/material.dart';

final darkTheme =
    ThemeData(primarySwatch: Colors.grey, primaryColor: Colors.black);
final lightTheme =
    ThemeData(primarySwatch: Colors.blue, primaryColor: Colors.pink);
