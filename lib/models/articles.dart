class Article {
  String name, description, url, photo, category, language, country;

  Article({
    this.name,
    this.description,
    this.url,
    this.photo,
    this.category,
    this.language,
    this.country,
  });
}
